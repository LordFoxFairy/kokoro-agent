---
architectureIndex: 1
rootId: agent.tools.media
owners:
  - "@LordFoxFairy"
---

# Media tools

- `create_image.py` — model-facing image intent only. Opaque Media authority and
  deterministic command identities are injected by the run assembly.

The tool does not select providers/models, settle credits, or expose storage URLs.
